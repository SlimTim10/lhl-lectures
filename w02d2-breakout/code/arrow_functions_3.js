function old_sum(x, y) {
  return x + y;
}

console.log(old_sum(2, 3));

const new_sum = (x, y) => {
  return x + y;
}

console.log(new_sum(2, 3));

const best_sum = (x, y) => x + y;

console.log(best_sum(2, 3));
